import 'package:flutter/material.dart';

class SunColors {
  static const mainBackground = Color(0xFFE9F1FD);

  static const backBuildings = Color(0xFF3381CC);
  static const sky = Color(0xFF3C97F4);
  static const sunSkin = Color(0xFFFDE25C);
  static const shadow = Color(0xFF31384B);
  static const window = Colors.grey;
}
